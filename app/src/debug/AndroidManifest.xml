<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <!-- Debug-only: lets ExtensionPublishTest subscribe like SeriesGuide does. Not exported;
             the extension addresses it from within this same app. -->
        <receiver
            android:name=".extension.DebugSubscriberReceiver"
            android:exported="false" />
    </application>

</manifest>
