<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- The app uses the Leanback UI on TV, but does not require it, so it also
         installs on phones and tablets. required=false keeps it available on all
         form factors; setting it to true would restrict the app to Android TV. -->
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />

    <!-- TVs are driven by a remote, not a touchscreen. Declaring the touchscreen
         as not required is mandatory for the app to be recognized as a TV app. -->
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:banner="@drawable/tv_banner"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:windowSoftInputMode="adjustResize"
        android:theme="@style/Theme.SteamBridge">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <!-- LAUNCHER makes the activity appear on phones/tablets; -->
            <!-- LEANBACK_LAUNCHER makes it appear on the Android TV home screen. -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
